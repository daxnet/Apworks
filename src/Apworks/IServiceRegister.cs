using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Apworks
{
    //public enum LifetimeStyle
    //{
    //    Transient,
    //    Singleton,
    //    PerWebRequest,
    //    PerWcfRequest,
    //    PerWcfSession
    //}

    public interface IServiceRegister
    {
        #region Commented
        
        //IServiceRegister RegisterServiceType<TFrom, TTo>();
        //IServiceRegister RegisterServiceType<TFrom, TTo>(string name);
        //IServiceRegister RegisterServiceType<TFrom, TTo>(LifetimeStyle lifetimeStyle);
        //IServiceRegister RegisterServiceType<TFrom, TTo>(string name, LifetimeStyle lifetimeStyle);
        //IServiceRegister RegisterServiceType(Type from, Type to);
        //IServiceRegister RegisterServiceType(Type from, Type to, string name);
        //IServiceRegister RegisterServiceType(Type from, Type to, LifetimeStyle lifetimeStyle);
        //IServiceRegister RegisterServiceType(Type from, Type to, string name, LifetimeStyle lifetimeStyle);
        #endregion

        
    }
}
